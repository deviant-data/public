# Static Viewer v4 Open Items

Handled:

- Converted v4 to render `streamlearn-viewer-state`.
- Replaced the copied renderer with a state-only renderer.
- Replaced copied proposal text.
- Added progress notes.

Remaining:

- Wire `streamlearn_viewer.py` output to a v4 Pages worktree.
- Keep generated static pages as the primary presentation path.
- Keep the data renderer as a fallback for missing generated pages.
